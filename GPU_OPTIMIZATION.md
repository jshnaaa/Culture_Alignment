# GPU 优化说明

本代码已经完全优化支持GPU训练，包含以下特性：

## 🚀 GPU 加速特性

### 1. 自动GPU检测
- 自动检测可用GPU数量和型号
- 显示GPU内存容量
- 智能选择最优执行设备

### 2. 模型加载优化
- **半精度加载**: 使用`torch.float16`节省50%显存
- **自动设备映射**: `device_map="auto"`自动分配多GPU
- **优化内存管理**: 自动清理GPU缓存

### 3. 混合精度训练 (AMP)
- 自动启用混合精度训练
- 显著节省GPU内存（约30-50%）
- 加速训练过程（约1.5-2倍速度提升）
- 保持训练精度不受影响

### 4. 内存管理
- 定期清理GPU缓存
- 实时监控GPU内存使用
- 非阻塞数据传输优化

## 📊 性能优化设置

```python
# 启用的优化设置
torch.backends.cudnn.benchmark = True      # 卷积优化
torch.backends.cuda.matmul.allow_tf32 = True  # TF32精度
torch.backends.cudnn.allow_tf32 = True     # 允许TF32
```

## 🖥️ 内存需求估算

### Llama 3.1-8B 模型：
- **CPU模式**: ~16GB 内存
- **GPU模式 (float16)**: ~8GB 显存
- **训练时**: 额外需要 4-6GB 显存用于梯度和优化器状态

### 建议配置：
- **最小配置**: 单张12GB GPU (如RTX 3060 12GB)
- **推荐配置**: 单张16GB+ GPU (如RTX 4080/4090)
- **最佳配置**: 多张GPU或专业卡

## 🔧 GPU 相关参数

### 在代码中可以调整的参数：

```python
# 批处理大小（根据GPU显存调整）
batch_size = 8  # 12GB GPU建议值
batch_size = 16 # 24GB GPU建议值

# 序列长度（影响显存占用）
max_length = 512  # 标准设置
max_length = 1024 # 需要更多显存

# 混合精度训练开关
use_amp = True    # 推荐开启
```

## 📈 性能监控

代码会实时显示：
- GPU型号和内存容量
- 当前GPU内存使用情况
- 训练速度和损失变化
- 每个epoch的详细指标

## 🐛 常见问题

### 显存不足 (CUDA Out of Memory)
**解决方案**：
1. 减少batch_size（从8改为4或2）
2. 减少max_length（从512改为256）
3. 启用混合精度训练（默认已启用）
4. 清理其他GPU进程

### 训练速度慢
**优化建议**：
1. 确保CUDA版本匹配
2. 使用SSD存储数据集
3. 启用数据预加载（已优化）
4. 考虑使用多GPU训练

### 精度下降
**检查项目**：
1. 学习率是否过大
2. 混合精度是否导致数值不稳定
3. 可以尝试关闭AMP: `use_amp=False`

## 💡 使用建议

1. **首次运行**: 建议使用小批量测试（batch_size=2）
2. **生产训练**: 根据GPU显存逐步增加batch_size
3. **多GPU**: 代码支持自动多GPU分配
4. **监控**: 关注GPU利用率和内存使用

